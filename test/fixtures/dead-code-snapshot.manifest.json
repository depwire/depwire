{
  "note": "Frozen graph snapshot for regression testing dead-code/health detector logic in isolation from live-tree drift (see fix/deadcode-relative-path-cwd-collision and fix/deadcode-relevantkinds-and-self-import). The graph is fixed; only the detector/scoring code under test should be able to move these numbers. Caveat 1: getPackageEntryPoints reads projectRoot's package.json from disk at test time (not frozen) -- if code-graph's own package.json 'exports' field changes, entry-point classification could shift independently of this snapshot. That's a narrow, deliberate-change-only risk, not the 'new files added elsewhere' drift this fixture is meant to guard against. Caveat 2: edge `key` strings in dead-code-snapshot.json (e.g. geid_<n>_<i>) embed a run-varying counter and are NOT byte-stable across regenerations from the same commit; verified by regenerating twice and diffing -- node keys, and edge (source,target,attributes) tuples ignoring the key field, were identical both times, and the derived `expected` block below was identical both times. Detector/health logic reads edge attributes, not edge keys, so this does not affect test correctness -- but do not diff the raw snapshot JSON byte-for-byte as a determinism check; compare node keys and edge (source,target,attributes) instead.",
  "sourceCommit": "4d502fc82c71d424aff315e6680c961b43ab2ac1",
  "sourceCommitNote": "fix: relevantKinds missing \"variable\"; add fixed-snapshot regression fixture; bump v1.11.0 -- the tracked tree at this commit is what parseProject() ran against. (An earlier draft of this manifest recorded nodeCount 5840, contaminated by three untracked scratch debugging scripts -- d-verify.mjs, lang-verify.mjs, m-verify.mjs -- left in the repo root from this session's investigation and picked up by parseProject() despite never being part of the tracked tree at this commit. Removed and regenerated; corrected nodeCount is 5817.)",
  "sourceRepo": "code-graph (self-scan)",
  "generatorVersion": "depwire-cli 1.11.0 (package.json version at generation time)",
  "generatedWithNode": "v25.2.1",
  "generatedAt": "2026-08-15T08:51:40.277Z",
  "nodeCount": 5817,
  "edgeCount": 2314,
  "expected": {
    "totalSymbols": 5817,
    "deadSymbols": 166,
    "deadPercentage": 2.85370465875881,
    "byConfidence": {
      "high": 166,
      "medium": 0,
      "low": 0
    },
    "healthOverall": 71,
    "healthGrade": "C",
    "orphansScore": 88,
    "orphansGrade": "B"
  }
}